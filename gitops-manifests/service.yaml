apiVersion: v1
kind: Service
metadata:
  name: sample-app-service
  namespace: default
  labels:
    app.kubernetes.io/name: sample-app
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: sample-app
